{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "setup": "composer install --ignore-platform-reqs && npm install && npm run build",
        "composer-dev": "composer install --optimize-autoloader --ignore-platform-reqs",
        "composer-prod": "composer install --no-dev --optimize-autoloader --ignore-platform-reqs",
        "fresh": "composer install --ignore-platform-reqs && php artisan migrate:fresh --seed"
    },
    "dependencies": {
        "@tailwindcss/vite": "^4.1.11",
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0",
        "tailwindcss": "^4.0.7",
        "vite": "^7.0.4"
    },
    "optionalDependencies": {
        "@rollup/rollup-linux-x64-gnu": "4.9.5",
        "@tailwindcss/oxide-linux-x64-gnu": "^4.0.1",
        "lightningcss-linux-x64-gnu": "^1.29.1"
    }
}
